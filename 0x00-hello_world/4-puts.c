#include <stdio.h>
/**
 * main - It is the entry point of a program where the execution of a program starts.
 *
 * Return: The return statement is used to end the execution of a function and returns control to the function from where it was called
 */
int main (void)
{
	puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
